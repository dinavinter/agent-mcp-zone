apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: {}
  labels:
    app: mcp-oauth
  name: mcp-oauth
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: mcp-oauth
  strategy:
    type: Recreate
  template:
    metadata:
      annotations: {}
      labels:
        app: mcp-oauth
    spec:
      containers:
      - args: []
        envFrom:
        - configMapRef:
            name: mcp-oauth
        image: '{{ .Values.images.mcpoauth }}'
        imagePullPolicy: Always
        name: mcp-oauth
        ports:
        - containerPort: 8080
          name: http
      imagePullSecrets:
      - name: image-pull-secret
      terminationGracePeriodSeconds: 180