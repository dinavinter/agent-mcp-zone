apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: {}
  labels:
    app: ai-core-proxy
  name: ai-core-proxy
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: ai-core-proxy
  strategy:
    type: Recreate
  template:
    metadata:
      annotations: {}
      labels:
        app: ai-core-proxy
    spec:
      containers:
      - args: []
        envFrom:
        - configMapRef:
            name: ai-core-proxy
        image: '{{ .Values.images.aicoreproxy }}'
        imagePullPolicy: Always
        name: ai-core-proxy
        ports:
        - containerPort: 3002
          name: http
      imagePullSecrets:
      - name: image-pull-secret
      terminationGracePeriodSeconds: 180